cmake_minimum_required(VERSION 2.8)
project( stereo_depth_perception )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( stereo_depth_perception stereo_depth_perception.cpp )
target_link_libraries( stereo_depth_perception ${OpenCV_LIBS} )
